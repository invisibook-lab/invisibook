# CKB 链上布局

Proof of Buy 在 CKB 上只占用两种 cell。本文规定它们的结构与验证规则，规定 L2 侧
必须自己完成哪些验证，以及两边的接口如何对应。

协议本身见 [proof_of_buy.md](proof_of_buy.md)，本文只谈落到 CKB 上的形态。

## 0. 设计约束

三条性质决定了下面所有取舍，先列出来：

**一笔交易的全部内容，在打包之前就对 L1 出块者可见。** data 如此，witness 同样如此
——witness 是交易的一部分，出块者要验证签名就必须读到它。白皮书 §9.6 要求出价内容
在入块前不可见，这一条就把"放进 witness 让脚本验一下"这条路彻底堵死了：凡是脚本
读得到的，正在出块的那个 L1 矿工也读得到。**出价上链时只能是一串看不懂的承诺。**

**脚本是验证器，不是计算器。** type script 只能看到当前这笔交易的 inputs、outputs、
cell_deps、header_deps 和 witnesses。它不能遍历链上状态，也就不能"扫描所有出价并
选出最大的"。

这两条叠加起来的结论是确定的：**L1 既看不见出价的内容，也扫不到出价的全集，因此
它不可能裁定谁赢。** 裁定只能在 L2 侧做。本布局据此把 L1 的职责压到最小。

**cell 的容量是长期锁定的。** 1 CKB = 1 字节，且只有销毁 cell 才退还。因此 cell
里只放**必须由链上状态承载的**数据，其余一律留在交易里——交易永久存在于 L1 区块中，
可供任何人事后核验，只是后续脚本读不到它。

## 1. L1 在协议里干什么

两件事，仅此而已：

| | L1 做什么 | 谁来验 |
| --- | --- | --- |
| 预付与分配 | 托管预付款，记录每个高度的支付承诺，保证它一经写入不可更改 | CKB 合约（R3.x） |
| 出价提交 | 给一串 32 字节的承诺盖一个"它在第几号区块上"的时间戳 | 无人验，只存（R5.1） |

**L1 不验证 VRF，不验证 goal，不验证任何 zk 证明，也不裁定分叉。** 它提供的是两样
L2 自己给不了自己的东西：一份不可篡改、不可回溯修改的支付记录，以及一份主链的备份
——每个区块的承诺在当时就留在了 L1 上，事后补写不进去。后者正是识别"事后花钱造一条
更高得分的分叉"所需的证据。剩下的全部判断由 L2 节点完成，清单见第 6 节。

这与白皮书 §8.2 的分工是同一句话：L1 备份，L2 裁定。

## 2. cell 总览

```
┌─────────────────────────────────────────────────────────────────┐
│  预算 cell            每个矿工一个，长期存在                      │
│    预付总额 + 高度到支付承诺的表                                  │
│    ↑ 矿工在出块前写入，写入后不可改，是 PoB 成本的链上凭据         │
├─────────────────────────────────────────────────────────────────┤
│  提交 cell            每个矿工每个高度一个，短期存在               │
│    一串 32 字节的承诺，别的什么都没有                             │
│    ↑ 矿工出块后提交，高度尘埃落定后自行回收容量                    │
├─────────────────────────────────────────────────────────────────┤
│  mining addr cell     全链唯一，长期存在，矿工的付款流入此处       │
│    带 mining_addr_type_script，钱流出时强制打标记                 │
│    ↑ 付款进来不受限，往外花才受约束                               │
├─────────────────────────────────────────────────────────────────┤
│  已用代币 cell        项目方从 mining addr 支出时产生，永久携带    │
│    与普通代币的唯一区别：带着一个洗不掉的类型脚本                  │
│    ↑ 流出 mining addr 时打上，此后永远回不去                      │
└─────────────────────────────────────────────────────────────────┘
```

没有锚点 cell，也没有结算交易。L1 不记录"结算到哪个高度"，因为它无从判断任何高度
的赢家；一个它算不出来的结论，写上去只会与 L2 节点独立算出的结果打架。

## 3. 预算 cell

矿工的预付款与分配计划。每个矿工一个，由矿工自己创建和维护。这是本布局里唯一有
实质规则的地方——白皮书 §7 的整套成本模型都压在它身上。

```
lock    secp256k1_blake160_sighash_all，args = blake160(矿工出块公钥)（R3.5）
type    budget_type_script
data
    prepaid       u64                     预付总额，单位 shannon
    allocations   [(u32, [32]byte)]       (L2 高度, Poseidon 支付承诺)
witness
    budget_proof  Groth16                 证明所有 amount 之和恰好等于 prepaid
```

承诺是 `Poseidon(amount, random)`，与 L2 侧 `consensus.PoseidonCommit` 同一参数化。
链上不打开这个承诺——它是给 L2 节点核对用的——但链上**要验证那个 zk 证明**，见 R3.2。

### data 的字节布局

```
  偏移   长度   内容
  0      8      prepaid            u64 小端
  8      4      分配表条目数        u32 小端      ┐ molecule fixvec
  12     36×n   条目               见下           ┘

  一个条目（molecule struct，定长 36 字节）
  +0     4      height             u32 小端
  +4     32     commitment         Poseidon 承诺
```

**`prepaid` 在 molecule 之外，是刻意的。** `budget_type_script` 每创建一个预算 cell
都要读一次总额（R3.3），把它放在最前面的裸 8 字节，脚本一次定长读取就拿到；包进
molecule table 的话，那 8 字节会变成 table 自己的长度与偏移头，脚本得先解析容器才能
取到它要的唯一一个字段。

**分配表用 molecule。** 它是这份 data 里唯一长度可变的部分，条目数写在头里，读的一方
无需按剩余字节倒推，长度对不上即判定这个 cell 不是它自称的形状。条目定长，所以用
fixvec——molecule 为变长向量准备的偏移表在这里是纯开销。

**条目按 height 升序，且不得重复。** 重复意味着同一个高度挂着两个承诺，而没有任何规则
可以在两者之间做选择；升序则让读的一方遇到第一个大于目标的高度就能停。编码与解码两侧
都强制这一条：表一次写定、永不可改（R3.1），编错就是一笔预付款白白扔掉。

Go 侧的编解码在 `chain/ckb/budget_data.go`。

**一张表一次写定，此后整个 cell 不可变。** 分配不是逐条追加的：矿工预付多少，就在创建
这个 cell 时一次性把它分配干净。想追加预算，就再建一个预算 cell，各自带各自配平的表。

### 验证规则

**R3.1 创建之后，cell 的 data 不可修改。** 预算 cell 只有两种合法转移：被创建，或者
被矿工用自己的 lock 整个花掉回收容量。没有"改一改"这种中间状态。

这一条是防后广播优势机制在链上的落点。白皮书 §7.3 要求矿工必须先承诺支付额、再计算
VRF；若矿工能在算完 VRF、发现 goal 不够高之后回头改大承诺，那个时序就失效了。链上
只有这一处能强制它。

**R3.2 分配必须配平：`Σ amount == prepaid`。** 创建时验证 witness 里的 Groth16 证明，
公开输入是 `prepaid` 与 `allocations` 里的全部承诺。

取等式而非"不超过"，一是电路便宜得多（不等式要做范围证明，等式只是一条线性约束），
二是语义更干净：钱已经真的付给 mining addr 了，"没分配完的部分"并不会退回来，声明
它的去向本就是矿工自己的事。

证明**不能**省掉对每个 `amount` 的范围约束：BN254 的标量域会回绕，不约束大小的话，
一组巨大的 amount 完全可以模 p 之后恰好等于 `prepaid`，等式照样成立而钱凭空多出来。

**R3.3 `prepaid` 必须等于同一笔交易中转入 mining addr 的容量。** 创建预算 cell 的
那笔交易里，要有一个 lock 等于 mining addr 的输出，其 capacity 恰好是 `prepaid`。
写一个比实际付款大的数就等于凭空印钱。

**钱不留在预算 cell 里。** 这一点容易看错。矿工付款的去向是 mining addr（第 4 节的
资金流第一段），预算 cell 只存那张表；它自己的 capacity 是占用链上存储所要锁定的
容量，连同这笔交易的手续费，都是矿工在 `prepaid` 之外自行承担的链上成本——押金在
把 cell 花掉时收得回来（R3.1），手续费付给 CKB 矿工。把这两者与 `prepaid` 混为一谈，
就会以为钱锁在 cell 里，而它早已不可追回。

**R3.4 预付款不得来自已用代币。** 交易的 inputs 里不能有带 `spent_type_script` 的
cell（见第 4 节）。少了这一条，第 4 节的整套标记形同虚设：项目方把代币从 mining addr
花出去、打上标记，转头就能拿这些被标记的代币来写自己的预算 cell 继续挖矿。

**R3.5 lock 必须是标准 sighash lock。** 预算 cell 的 lock 只能是
`secp256k1_blake160_sighash_all`，其 args 是所有者公钥的 blake160。

这一条是第 6 节 V7 第一项的前提。那一项要求 L2 区块的 `miner_pubkey` 与这个 cell 的
所有者是同一把密钥；若 lock 的形式不固定——换成多签，或某个自定义脚本——args 里根本
不存在"单一公钥"这回事，"同一把密钥"也就没有可比对的落点。

**链上只能管到形式，管不了身份。** CKB 看不见 L2 区块，不知道出块公钥是谁，所以
"args 是否等于 `blake160(miner_pubkey)`"只能由 L2 节点去比。链上把 lock 的形式定死，
链下那一比才成立——两边缺一不可。

要绑的是**这张表归谁用**，不是谁付的钱。出钱的可以另有其人：A 自愿掏钱、把预算 cell
的 lock 设成 B 的，那是 A 的授权；冒领指的是 B 拿 A 的表去出块，而那由 lock 挡住。

早前设想过一个 `cleared_below` 水位，用来清理已经用过的旧条目。`== prepaid` 之下它
没有存在理由了：整张表一次写定、永不修改，"清理"只剩把整个 cell 花掉这一种形式，
而那由 lock 管就够了。

### 总额明文，每个高度的出价是承诺

`prepaid` 是明文，`allocations` 里每个高度对应的只是一串 Poseidon 承诺。这与白皮书
§7.2 一致，也是 CKB 的形态使然：容量转移本就是公开的，一笔预付款有多少，链上谁都查
得到，藏不住也不必藏。

要藏的是**每个高度出多少**。竞争者若能提前知道你为某个高度出了多少，§7.3 的时序保护
就落空了；而只知道你的总预算，推不出你在任何一个具体高度上的出价。

代价是矿工的总投入规模对外可见。若将来连总额也要隐藏，需要 CKB 侧具备隐私转账能力，
属于另一个议题。

## 4. 已用代币 cell

白皮书 §9.4 的落点。mining addr 持续吸纳矿工付来的代币，这让项目方处在一个特殊位置：
它自己挖矿时，钱是付给自己的，除 L1 手续费外没有真实成本，而 VRF 挡不住它化身几千个
矿工把诚实矿工的中选概率稀释掉。

防御不是禁止项目方花钱，而是**给花出去的钱打上洗不掉的标记，让它再也回不到 mining
addr**：

```
  矿工支付 ────▶  mining addr（干净）
                        │
                   项目方支出
                        │
                        ▼
                 已用代币 cell（带 spent_type_script）
                        │
            ┌───────────┴───────────┐
            ▼                       ▼
     开发、运营、生态资助      回不去 mining addr，
                              也写不了预算 cell
```

```
lock    任意（花给谁就是谁的）
type    spent_type_script
data    空
```

### 验证规则

**R4.0 流出 mining addr 必须打标记。** 由 `mining_addr_type_script` 守在 mining addr
的 cell 上：消耗它时，每个 output 要么带 `spent_type_script`，要么仍是 mining addr
自己（后者是找零，钱没真正离开）。

这一条是标记的**来源**，没有它下面两条都落空——R4.1 与 R4.2 管的都是"标记已经存在
之后"，而 mining addr 若只是个普通 lock，项目方拿私钥一签、输出一个干净 cell，那两条
规则一条也碰不到。整套防御会被一个签名绕过去。

因此 mining addr 必须从一开始就带上这个 type script。矿工付款进来不受任何限制（钱是
干净的），规则只在往外花的那一刻生效。

**R4.1 标记只增不减。** 任何消耗已用代币 cell 的交易，其 outputs 中**每一个**
capacity 有来源于该 input 的 cell，都必须同样带上 `spent_type_script`。

这一条是标记"洗不掉"的全部依据。少了它，项目方付给 A、A 再付给 B，两手之后标记就没
了。要求它向所有后继传播，才堵得住这条路——白皮书说的"无法通过混币等抹除交易历史的
手段绕过"，指的就是这个。

**R4.2 不得流回 mining addr。** outputs 中不能有 lock 等于 mining addr 的 cell。

**R4.3 不得用于预付。** 由 R3.4 在预算 cell 那一侧强制，这里不重复检查——预算 cell
的脚本本来就要遍历自己的 inputs，让它顺手看一眼比在这里反向追踪便宜得多。

### mining addr 写在哪里

`spent_type_script` 要判断"是不是流回了 mining addr"，就得认得这个地址。它作为
`args` 传进脚本，而不是硬编码在代码里——同一份脚本代码因此可以服务多条 L2 链，各自
用不同的 args 部署。

代价是这个 args 一经部署就不能改：改了就是另一个脚本、另一份标记，旧标记管不住新
地址。**mining addr 因此是一次性选定、终身不可更换的。** 这一点要在上线前想清楚，
它比看上去更重。

### 这条防御的边界

链上能做的到此为止：代币被标记、回不去、用不了预付。剩下的是市场行为——项目方可以
拿被标记的代币去市场上换普通代币。白皮书 §9.4 论证过这条路不可持续：被标记代币用途
受限，兑换率普遍高于一比一，且 L2 越有价值兑换率越高。链上机制不试图禁止兑换，只负责
让它必须发生在链下、且必须付出溢价。

## 5. 提交 cell

矿工对某个 L2 高度的出价，以承诺形式提交。每个矿工每个高度一个，各建各的，互不
消耗——这样同一个 L1 区块内的并发提交不会相互冲突。

```
lock    矿工可支配的 secp256k1 key（随时可自行回收容量）
type    commit_type_script
data
    commitment   [32]byte    SHA256(l2_block_hash || random)
```

**承诺的内容只有 L2 区块哈希。** 哈希已经唯一确定了整个区块——高度、`goal`、VRF
输出、父链接、每一笔交易都在里面——再把其中几项单独塞进承诺，等于把同一件事承诺
两遍。揭示时给出 `(l2_block_hash, random)` 就够：拿到区块的人先核对开启值与链上
承诺相符，再核对区块与哈希相符。

**用 SHA256 而不是 Poseidon。** 这个承诺不进任何电路：链上只记录、不验证，揭示由
L2 节点把两个字节串拼起来哈希一次就能核对。Poseidon 的 zk 友好在这里没人用得上，
代价却要照付——它的输入是 BN254 域元素，32 字节的哈希塞不进去，就得拆成两半，两边
还要约定拆法一致、参数化一致。换成字节串哈希，这两个问题都不存在。

预算 cell 里的支付承诺是另一回事，仍然用 Poseidon（第 3 节）：那些开启值确实要进
预算上限证明的电路。

### 验证规则

**R5.1 data 恰好 32 字节。** 这是这个脚本的全部职责。

**R5.2 消耗不受 type script 约束。** 高度尘埃落定之后，矿工凭 lock 花掉这个 cell
取回容量。链上没有结算交易会来消耗它。

### 为什么一条实质规则都没有

data 里只有一串哈希，链上没有任何东西可供比对：承诺的开启值不在这笔交易里（在也
不行，见第 0 节第一条），预算 cell 里的支付承诺同样是密文，VRF 的输入是 L2 的父
区块哈希而 L1 不知道 L2 的链长什么样。**没有明文，就没有可验之物。**

这不是把验证省掉了，是把它挪到了唯一有能力做的地方。L2 节点手里有完整的 L2 链、
有矿工广播的开启值、有 L1 上的承诺与支付记录，三者对齐才是一次完整的验证；L1 只
持有其中一份，验不出什么有意义的结论。

代价是 L1 上出现无效承诺不要钱——谁都可以创建一个内容荒谬的提交 cell。这不构成
攻击：L2 节点在校验开启值时会拒绝它，而攻击者白付了一笔 L1 手续费和一份容量占用。

### 承诺藏住了什么，没藏住什么

藏住的是出价的内容。没藏住的是"某个地址提交了一笔东西"——交易的输入来自矿工自己的
钱包，出块者据此能推断提交者是谁；预算 cell 的 allocations 也公开写着谁在为哪些
高度备战。

所以这层防御挡住的是**按出价高低定向丢弃**，挡不住按身份定向丢弃。后者的代价高得
多：它要长期持续地针对同一批地址，而矿工换一批地址与资金来源就重新变得不可
辨认，被针对的又恰好是最容易察觉自己被针对的人。

## 6. L2 侧必须自己完成的验证

L1 交出来的只是"哪些承诺在第几号区块上"。把它变成"这个高度谁赢"，是 L2 节点的事。
早前设想放在合约里的那套规则，一条不少地落到这里。

### 揭示走 L2 网络

承诺入块之后，矿工在 L2 的 p2p 网络里广播开启值：

```
l2_block_hash, random                       打开 L1 上的区块承诺
l1_block_hash, tx_idx                        这笔承诺落在 L1 的哪里
amount, payment_random                       打开预算 cell 里的支付承诺
vrf_output, vrf_proof, miner_pubkey, block_sig, prev_hash
```

`l1_block_hash` 与 `tx_idx` 不是冗余信息，它们扛两件事：

- **定位。** 没有它们，验证者只能扫遍 L1 去找哪个提交 cell 的 data 等于这串承诺，
  代价是 O(L1 链长)。有了它们，取块、取交易、比对，三步而已。
- **定时。** §8.2 识别"事后买出来的分叉"靠的就是每笔承诺**何时**上链。位置给出时间
  锚点，而用哈希而非高度还顺带检测了重组：哈希不在当前 L1 主链上，就说明这笔承诺已
  随重组消失，该分支上的这个区块也就不再算数。

开启值从不上 L1。它必须与 L2 区块一起持久化，新节点同步时要能从对等节点一并取到
——丢了开启值，那个高度的赢家就无从复原。

### 校验清单

收到一组开启值，节点依次检查：

**V1 承诺存在且已入块。** 按 `l1_block_hash` 取出那个 L1 区块（取不到，或它不在当前
L1 主链上，都判不通过），按 `tx_idx` 取出那笔交易，在它创建的 cell 里找出带
`commit_type_script` 的那个，要求其 data 等于重算的 `SHA256(l2_block_hash || random)`。

一笔交易理论上可以创建不止一个提交 cell，所以定位到交易之后仍要在它的 outputs 里找
匹配的那个——这是一次极短的遍历，不值得再为它引入一个 `output_idx` 字段。

记下该 L1 区块的高度：它是这个区块"何时被锚定"的凭据，§8.2 靠它识别事后伪造的分叉。

**V2 分支连续。** 这个区块必须挂在一条从分叉点起逐高度相接、中间没有缺口的链上。
缺一个高度，整条分支就失去参选资格——不能把各个高度上得分最高的区块挑出来拼成
一条自己的链。

**没有时间窗口这一说。** 承诺来得早晚不决定它算不算数：一笔迟到的承诺照样合法，
它只是形成了一条很短的分支，在累计得分上赢不了。用不着一条规则去挡它，理由见本节
末尾的「分叉选择与终局」。

**V3 支付承诺匹配。** 读该矿工预算 cell 在 `l2_height` 上的条目 `c`，要求
`Poseidon(amount, payment_random) == c`。

**V4 支付承诺写得够早。** 写入 `c` 的那笔 L1 交易，必须比**这个区块自己的承诺**所在
的 L1 区块早至少 24 个 L1 区块。这是白皮书 §7.3 的支付前移，链上的 R3.1 只保证整张
表一经写定就不可改，"写得够早"要在这里查。

**V5 VRF 有效。** 以 `miner_pubkey` 为公钥、`prev_hash` 为输入验证
`(vrf_output, vrf_proof)`。VRF 公钥就是出块公钥，矿工无法用一次性密钥碾出有利输出。

**V6 goal 正确。** `goal == f(amount, vrf_output)`，`f` 取乘法，`vrf_output` 前 8
字节按大端解释为 u64。即 `consensus.CalcBlockScore`。

**V7 身份一致。** 预算 cell 的 lock args、`miner_pubkey`、`block_sig` 的签名者是
同一把密钥。矿工不能拿别人的支付来为自己出价。

比对方式是 `blake160(miner_pubkey) == 预算 cell 的 lock args`——CKB 标准 lock 的
args 是公钥的 blake160（20 字节），不是压缩公钥本身，与 `miner_pubkey` 和 `block_sig`
那两项直接比公钥不同。lock 的形式由 R3.1 那一节的 R3.5 定死，否则这一比无从下手。

**这一项是整套支付校验的地基，不是补充。** 少了它，V3 拿来比对的那个承诺就是矿工自己
给的：他编一个承诺，再配一套能打开它的 `(amount, payment_random)`，Poseidon 照样对得
上，`amount` 随他写多大，`goal` 也就随之膨胀。V3 会退化成"自述与自述一致"。

**V8 父链接属实。** `prev_hash` 必须是本节点认定的 `l2_height - 1` 的区块哈希。
全部通过，这个区块才是它所在分叉上的合法一环。选哪条分叉、何时不可回滚，见下一节。

### 分叉选择与终局

**分两步，且顺序不可换。**

```
  第一步  准入   一个区块进入记录，当且仅当它的承诺已在某个 L1 区块里、
                 且开启值已在 L2 网络上广播
                       │
                       ▼
  第二步  裁定   在准入的区块里，取累计 goal 最大且连续的分叉为主链
```

第一步不比大小，只问"当时在不在"。承诺进 L1 区块时就带上了那个区块的高度，事后花钱
造出来的分叉拿不出这个——它的承诺只可能是新近才上链的（§8.2）。第二步不问来历，只
在第一步放行的区块里求和。

**倒过来做就是另一回事。** 没锚定的区块造起来不要钱，一条塞满这种区块的分支可以顶着
最大的总分，而其中 L1 能背书的几乎没有。先比总分，这条分支就赢下了比较，节点随后把
它那段短短的已锚定前缀**不可逆地**落盘，压过一条锚定更长、也确实付过钱的对手分叉。
实现见 `consensus.eligibleForks` 与紧随其后的 `ChooseFork`。

**第二步的规则：取累计 `goal` 最大且区块连续的分叉。**

```
  分叉 A   h ── h+1 ── h+2 ── h+3        Σgoal = 410  ◀── 主链
  分叉 B   h ── h+1 ──  ✗  ── h+3        有缺口，不参选
  分叉 C   h ── h+1 ── h+2               Σgoal = 260
```

连续不是修饰语。允许缺口，就等于允许攻击者从每个高度挑出自己得分最高的那块拼成
一条分支——而那些高度上他本来是输的。要求逐高度相接，他就必须为每一块都付钱。

累计得分完全相同时，取分叉点后第一个区块哈希字典序小者。概率极低，但需要一条确定
的断路规则，否则不同节点会各执一词。

不可回滚性随累计得分的差距增长，与中本聪共识同理：迟到的翻案得拿累计得分说话，而那
意味着把之后每一个高度都重新买一遍。

L1 在这里堵的是另一条路：单看得分，"当时就存在的链"与"事后花钱买出来的链"没有区别，
而原主链每个区块的承诺当时就写进了 L1，事后造的那条只可能有新近才上链的承诺。节点
对照 L1 的记录就能认出来。详见白皮书 §8.2。

### 这样做付出了什么

**L1 的历史不再自证 L2 的历史。** 重放 CKB 只能得到一堆无法解读的哈希；要复原"哪
条链赢了"，必须同时拿到 L2 网络里的开启值。

好在攻击者能做的有限：承诺是绑定的，**没人能编造出一个与链上承诺相符的假开启值**，
所以伪造一段 L2 历史是不可能的。可能的只有**隐瞒**——只把一部分开启值给某个新同步
的节点，让它算出一条累计得分较低的主链。这要求攻击者是该节点唯一的数据来源；从多个
对等节点取数即可化解。白皮书 §8.3 的长程攻击防御因此基本成立，但成立的理由变了：
不是"主链写在 L1 上"，而是"L1 上的承诺让历史无法被改写，重写就得重新付一遍钱"。

这是弱主观性。它的边界见第 8 节。

## 7. L2 侧接口映射

`chain/consensus/` 中三个接口与本布局的对应关系。三者的 CKB 实现都在
`chain/ckb/`，由一个 `ckb.Client` 同时承担——在链上它们是同两种 cell 的三个视图，
拆成三份就是三条连接、三份脚本配置，以及三次让这些配置彼此走样的机会。

节点在 `[ckb] enabled = true` 时接上真实 CKB，否则退回内存里的 mock L1。脚本地址
逐链部署，由 `chain/cmd/ckb-deploy` 发布并打印整段配置。

### L1PaymentVerifier

```go
FetchPrepayment(ctx, txHash, minerPubkey) (prepaid *big.Int, err error)
FetchAllocation(ctx, txHash, minerPubkey, height) (*Allocation, error)
```

读该矿工由 `txHash` 创建的那个预算 cell，分别返回**明文**的 `prepaid` 与
`allocations` 中 `height` 对应的 Poseidon 承诺，查不到返回 `ErrPaymentNotFound`。

总额明文、每条出价是承诺，两者形态不同，见第 3 节：容量转移本就公开，`prepaid` 藏
不住也不必藏；要藏的是每个高度的 `amount`。

**`txHash` 不能省。** 一个矿工可以有多个预算 cell——想追加预算就再建一个（第 3
节）——只凭 `minerPubkey` 定位不唯一，`txHash` 指明用的是哪一笔预付。

**V7 的第一项在这里落实。** `minerPubkey` 不只是查询用的键：两个方法都会读出预算
cell 的 lock，确认它的 code hash 是标准 sighash lock（R3.5 给的前提），并比对 args
是否等于 `blake160(minerPubkey)`，不符即 `ErrPaymentNotFound`。少了这一步，V3 比对
的承诺就是矿工自己给的——他编一个承诺、配一套能打开它的 `(amount, payment_random)`，
Poseidon 照样对得上，`amount` 随他写多大——整条支付校验退化成自证。

### L1CommitmentSubmitter

```go
SubmitCommitment(ctx, commitment *BlockCommitment) (l1TxHash string, err error)
```

构造并发送提交交易：抽一个 32 字节随机数，用 `consensus.CommitBlockHash` 算
`SHA256(l2_block_hash || random)`，创建提交 cell。`BlockCommitment` 里只有这串承诺
会上链，高度字段仅供本地记账。交易里不带任何与出价有关的其他数据——带了就等于没做
这层防御。

`random` 必须落盘保存：它是开启承诺的唯一钥匙，丢了这一笔出价就作废，付出去的 L1
代币也换不回对应的出块权。实现落在 `store.Bids`，且**先写盘再提交**：反过来的话，
一次崩溃就会在 L1 上留下一个谁也打不开的承诺。重发走 `resubmitCommitment`，复用已
存的那份开启值——换一个随机数就等于把旧承诺彻底作废。

承诺入块后，矿工还要在 L2 网络广播第 6 节那组开启值。这是 p2p 层的动作，不属于这个
接口，但必须与提交严格配对：**提交了不揭示等于没出价，揭示了对不上承诺会被全网拒绝。**

```go
CommitmentOnChain(ctx, l1TxHash) (*L1Location, error)
// L1Location{ BlockHash string; TxIdx uint32 }
```

`get_transaction` 取状态，返回这笔提交落在哪个 L1 区块的第几笔交易，没上链则返回
nil。

nil 只驱动一个决定：重发——提交要么从未落地，要么随 L1 重组消失了，从这里看是同一
回事，补救也是同一个。

**位置不是顺带给的。** 之后核验这个开启值的人得在 L1 上找到这笔承诺，没有位置就只能
扫链；区块哈希同时给这次锚定盖上时间戳，而 §8.2 正是靠它把"当时就在的分叉"与"事后
买出来的分叉"区分开。矿工把它连同开启值一起存进 `store.Bids`，揭示时随开启值广播，
收到的一方拿它去调 `L1Reader.CommitmentAt`。

### L1Reader

```go
CommitmentAt(ctx, loc *L1Location) (*AnchoredCommitment, error)
// AnchoredCommitment{ Commitment string; L1Height uint64 }
BudgetOwner(ctx, txHash) (args []byte, err error)
```

`CommitmentAt` 就是 V1：按 `loc.BlockHash` 取 L1 区块，确认它仍在 L1 主链上（不在就
返回 `ErrNotOnChain`），取第 `loc.TxIdx` 笔交易，在它创建的 cell 里找出带
`commit_type_script` 的那个，返回它的 32 字节 data 与该 L1 区块的高度。

**按位置读，而不是按高度扫。** 提交 cell 里只有一串承诺，没有 L2 高度——§5 说的"别的
什么都没有"是字面意思——所以链上根本没有"某个 L2 高度的全部提交"这种查询可做。开启值
自带位置，节点据此逐个核验，扫链既不必要也办不到。

`BudgetOwner` 返回预算 cell 的 lock args，供 V7 第一项比对。返回的是原始 args 而非
公钥：CKB 标准 lock 的 args 是公钥的 blake160，哈希在调用方那一侧算。


## 8. 已定与待确认

### 已定

**L1 只存不验。** 链上没有 VRF 验证，没有 zk 验证，没有 Poseidon 计算，没有裁定。
理由见第 0 节与第 5 节。

**容量占用是 O(1)。** 一个预算周期结束后，矿工把整个预算 cell 花掉取回容量；提交 cell
同样由矿工自行回收。链上没有任何随高度累积的长期状态。

**终局是准入加裁定两步（第 6 节）。** 承诺上 L1 加揭示，管的是这个区块进不进记录，
它给区块盖上"当时确实在"的时间戳，堵的是长程攻击；累计得分管的是在记录里的区块中
哪条分叉是主链。两者不冲突也不重复，是一条流程的两步，顺序固定：先筛后排。

不可回滚性随累计得分的差距增长；迟到的提交自己就赢不了，无须额外规则去挡。

**本布局的时间参数只有一个：白皮书 §7.3 / §9.3 的 24 个 L1 区块。** 支付承诺要比用它
出块的时刻提前这么久（对应 V4），防的是从 L1 发起的自私挖矿。

### 待确认

- **开启值的传播与留存。** 它现在是共识的必需品而非附属信息：新节点同步时必须能连
  同区块一起拿到历史开启值。要定的是它放在 L2 区块的哪个位置、由谁负责补发，以及
  一个矿工提交了却迟迟不揭示时，那笔承诺如何被当作弃权处理。
- **L1 重组把承诺带走之后。** 一个区块的承诺所在的 L1 区块若被重组掉，它就退回"未上链"
  的状态，需要重发。要确认的是：节点在重发与重新计入之间如何保持一致，以及暂存写入
  若已落盘该怎么办。
- **弱主观性的边界。** 攻击者无法伪造开启值，只能隐瞒。需要量化：一个新节点要连到
  几个诚实对等节点才足够安全，以及是否值得提供一个可选的检查点机制。
- **无效承诺的成本。** 链上不验，创建一个垃圾提交 cell 只需手续费与可退还的容量。
  正常情况下无害，但需要估算它作为 L1 状态膨胀手段的成本下界。
