[package]
name = "netdelay"
version = "0.1.0"
edition = "2021"
description = "UDP relay that adds a fixed one-way delay and a rate cap (RQ2)"

[[bin]]
name = "netdelay"
path = "src/main.rs"

[dependencies]

[profile.release]
opt-level = 3
