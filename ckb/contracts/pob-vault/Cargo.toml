[package]
name = "pob-vault"
version = "0.1.0"
edition = "2021"

[dependencies]
# ckb-std and nothing else: this script only compares hashes.
ckb-std = "1.1"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
